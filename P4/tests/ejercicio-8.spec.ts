import 'mocha';
import {expect} from 'chai';
import {pointsAdd} from '../src/ejercicio-8';

type point = {
    x: number;
    y: number;
  }

describe('pointsAdd function tests', () => {
  // eslint-disable-next-line max-len
  it('pointsAdd(firstPointToIntroduce, secondPointToIntroduce) returns value ', () => {
    // eslint-disable-next-line max-len
    // expect(pointsAdd({x: 0, y: 0}, {x: 1, y: 1})).to.be.equals({x: 1, y: 1});
  });
});

import {pointsSubtract} from '../src/ejercicio-8';

describe('pointsSubtract function tests', () => {
  it('pointsSubtract() returns value ', () => {
    // expect(pointsSubtract()).to.be.eq();
  });
});

import {productOperation} from '../src/ejercicio-8';

describe('productOperation function tests', () => {
  it('productOperation() returns value ', () => {
    //   expect(productOperation()).to.be.eq();
  });
});

import {euclideanDistance} from '../src/ejercicio-8';

describe('euclideanDistance function tests', () => {
  it('euclideanDistance() returns value ', () => {
    // expect(euclideanDistance()).to.be.eq();
  });
});
